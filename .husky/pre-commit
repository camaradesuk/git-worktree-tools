#!/bin/sh
set -e

# Run TypeScript type checking first (catches type errors that eslint/prettier miss)
pnpm exec tsc --noEmit

# Check formatting on all files (catches files modified after staging)
pnpm exec prettier --check .

# Then run lint-staged for eslint and prettier fixes on staged files
pnpm exec lint-staged
