---
phase: 07-legacy-cli-wiring-completeness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/wtconfig.ts
  - src/cli/wt/config.ts
  - src/cli/wtconfig.test.ts
autonomous: true
requirements:
  - LOG-01
  - LOG-04
  - UNI-01

must_haves:
  truths:
    - 'Running GWT_LOG_LEVEL=debug wtconfig produces debug output through the shared logger'
    - 'Running wt config show --json produces clean JSON without decorative text'
    - 'All decorative output in wtconfig.ts and wt/config.ts uses UI primitives, not raw console calls'
    - 'wtconfig --verbose and --quiet flags control logger verbosity'
  artifacts:
    - path: 'src/cli/wtconfig.ts'
      provides: 'Logger wiring + full UI primitive migration for wtconfig'
      contains: 'initializeLogger'
    - path: 'src/cli/wt/config.ts'
      provides: 'setJsonMode call + UI primitive migration for wt config handler'
      contains: 'setJsonMode'
  key_links:
    - from: 'src/cli/wtconfig.ts'
      to: 'src/lib/logger.ts'
      via: 'initializeLogger call in main()'
      pattern: "initializeLogger\\("
    - from: 'src/cli/wt/config.ts'
      to: 'src/lib/ui/output.ts'
      via: 'setJsonMode call in handler'
      pattern: "setJsonMode\\("
    - from: 'src/cli/wtconfig.ts'
      to: 'src/lib/ui/index.ts'
      via: 'print/printErr/printStatus/printError/printDim replacing console calls'
      pattern: "print(Status|Error|Dim)?\\("
---

<objective>
Wire initializeLogger() into wtconfig.ts, add setJsonMode() to wt/config.ts, and migrate all ~160 raw console calls across both files to UI primitives.

Purpose: Close integration gaps INT-02 (wtconfig logger), INT-05 (wtconfig UI migration), and INT-06 (wt/config.ts setJsonMode). These are the two large files in the phase with ~180 combined console calls.
Output: Both config files fully wired with logger and UI primitives. Tests updated to verify.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-legacy-cli-wiring-completeness/07-CONTEXT.md
@.planning/phases/07-legacy-cli-wiring-completeness/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/logger.ts:

```typescript
export function initializeLogger(options?: LoggerOptions): void;
// LoggerOptions: { verbose?: boolean, quiet?: boolean, noColor?: boolean, json?: boolean, commandName?: string }
```

From src/lib/colors.ts:

```typescript
export function setColorEnabled(enabled: boolean): void;
```

From src/lib/ui/index.ts:

```typescript
export { setJsonMode, isJsonMode, print, printErr } from './output.js';
export {
  printStatus,
  printHeader,
  printDetail,
  printDim,
  printNextSteps,
  printSummaryBox,
} from './status.js';
export { printError, errorToDisplay } from './error.js';
// printError({ title, detail?, hint? }): void  -- to stderr, JSON-mode aware
// printStatus(level, message): void  -- to stdout via print(), JSON-mode gated
// printDim(message): void  -- dim text to stdout via print(), JSON-mode gated
// print(message): void  -- to stdout, silenced in JSON mode
// printErr(message): void  -- to stderr, silenced in JSON mode
```

Console call inventory (from research):

- wtconfig.ts: ~85 console.log, ~28 console.error, 3 console.warn = ~116 total
  - ~10 are JSON output (formatJsonResult/JSON.stringify) -- KEEP as console.log
  - ~106 decorative -- MIGRATE to UI primitives
- wt/config.ts: ~45 console.log, ~16 console.error, 2 console.warn = ~63 total
  - ~10 are JSON output (formatJsonResult) -- KEEP as console.log
  - ~53 decorative -- MIGRATE to UI primitives

Migration rules:

- console.error(colors.error(msg)) -> printError({ title: msg })
- console.error(colors.error(msg)) + console.error(colors.dim(hint)) -> printError({ title: msg, hint: hint })
- console.log(colors.success(msg)) -> printStatus('success', msg)
- console.log(colors.info(msg)) -> printStatus('info', msg)
- console.log(colors.dim(msg)) -> printDim(msg)
- console.log(plainText) -> print(plainText) (JSON-mode gated)
- console.log() -> print('')
- console.warn(colors.warning(msg)) -> printErr(colors.warning(msg)) (stderr, JSON-mode gated)
- console.log(formatJsonResult(...)) -> KEEP AS IS (this IS the structured output)
- console.log(JSON.stringify(...)) -> KEEP AS IS (JSON migrate output)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Wire logger into wtconfig.ts and migrate all console calls to UI primitives</name>
  <files>src/cli/wtconfig.ts</files>
  <action>
**INT-02: Logger wiring for wtconfig.ts**

1. Add imports at top of file:

   ```typescript
   import { initializeLogger } from '../lib/logger.js';
   import { setColorEnabled } from '../lib/colors.js';
   import {
     print,
     printErr,
     printError,
     printStatus,
     printDim,
     setJsonMode,
   } from '../lib/ui/index.js';
   ```

2. Where `args` and `command` are currently parsed at module level (lines 68-70), also parse logger flags:

   ```typescript
   const verbose = args.includes('--verbose');
   const quiet = args.includes('--quiet');
   const noColor = args.includes('--no-color');
   ```

3. In `main()`, after `printDeprecationNotice('wtconfig', 'wt config')` (line 85) and before the switch statement (line 86):
   ```typescript
   initializeLogger({
     verbose,
     quiet,
     noColor,
     json: jsonMode,
     commandName: 'wtconfig',
   });
   setJsonMode(jsonMode);
   if (noColor) {
     process.env.NO_COLOR = '1';
     setColorEnabled(false);
   }
   ```

**INT-05: Full UI primitive migration for wtconfig.ts (~106 decorative calls)**

Migrate function by function. The key principle: `console.log(formatJsonResult(...))` and `console.log(JSON.stringify(...))` stay as raw console.log (they ARE the JSON output). Everything else migrates.

**showHelp() function (lines 138-177):**

- This is a large template literal with `console.log(...)`. Replace with `print(...)`. The help text is decorative and should be JSON-mode gated. The `colors.cyan()`, `colors.warning()` calls inside the template are fine -- they produce strings that `print()` outputs.

**showConfig() function (lines 179-211):**

- Line 190: `console.log(formatJsonResult(result))` -> KEEP (JSON output)
- Line 196: `console.log(colors.info('Current Configuration'))` -> `printStatus('info', 'Current Configuration')`
- Line 197: `console.log()` -> `print('')`
- Lines 200-201: `console.log(colors.dim(...))` -> `printDim(...)`
- Line 204: `console.log(colors.dim(...))` -> `printDim(...)`
- Line 205: `console.log()` -> `print('')`
- Line 210: `console.log(mergedDisplay)` -> `print(mergedDisplay)`

**setConfig() function (lines 312-374):**

- Lines 314-316: `console.error(colors.error(...))` + `console.error(colors.dim(...))` -> `printError({ title: 'Usage: wtconfig set <key> <value>', hint: 'Example: wtconfig set baseBranch develop' })`
- Line 320: `console.error(colors.error(...))` -> `printError({ title: \`Missing value for key: ${key}\` })`
- Lines 348-351: Validation error block -> `printError({ title: 'Configuration validation failed:' })` then loop using `printErr(colors.error(\` ${error.path}: ${error.message}\`))`
- Lines 356-358: Warnings -> `printErr(colors.warning(...))`
- Line 363: `console.log(colors.success(...))` -> `printStatus('success', ...)`
- Line 366: `console.log(colors.success(...))` -> `printStatus('success', ...)`
- Lines 369-371: Catch error -> `printError({ title: \`Failed to set value: ...\` })`

**getConfig() function (lines 376-431):**

- Lines 386-387: `console.error(colors.error(...))` + `console.error(colors.dim(...))` -> `printError({ title: 'Usage: wtconfig get <key>', hint: 'Example: wtconfig get ai.provider' })`
- Line 409/411: JSON output -> KEEP
- Line 411: `console.error(colors.error(...))` -> `printError({ title: \`Unknown configuration key: ${key}\` })`
- Lines 422-423, 427-429: JSON and value output -> console.log for JSON (KEEP), `print(JSON.stringify(...))` and `print(String(value))` for non-JSON

**editConfig() function (lines 433-485):**

- Line 468: `console.log(colors.dim('Cancelled.'))` -> `printDim('Cancelled.')`
- Line 477: `console.log(colors.dim(...))` -> `printDim(...)`
- Line 482: `console.error(colors.error(...))` -> `printError({ title: 'Failed to open editor' })`

**validateCurrentConfig() function (lines 487-549):**

- Line 500: JSON output -> KEEP
- Line 502: `console.log(colors.success(...))` -> `printStatus('success', 'No configuration file found. Nothing to validate.')`
- Line 525: `console.log(colors.info(...))` -> `printStatus('info', ...)`
- Line 528: `console.log(colors.success(...))` -> `printStatus('success', 'Configuration is valid.')`
- Lines 533-536: Error output -> `printErr(colors.error(...))` for each line
- Lines 540-543: Warning output -> `printErr(colors.warning(...))` for each line

**runMigrateCommand() function (lines 551-673):**

- Lines 562-565: JSON output -> `console.log(JSON.stringify(...))` KEEP
- Lines 564-565: `console.error(colors.error(...))` + `console.error(colors.dim(...))` -> `printError({ title: 'Error: Not in a git repository', hint: 'Run this command from within a git repository.' })`
- Lines 576-577: JSON output -> KEEP
- Line 583: JSON output -> KEEP
- Lines 588-593: JSON output -> KEEP
- Lines 599-607: JSON output -> KEEP
- Line 617: `console.log(colors.success(...))` -> `printStatus('success', 'Config is up to date, no migration needed.')`
- Line 622: `console.log(formatMigrationReport(...))` -> `print(formatMigrationReport(...))`
- Line 623: `console.log()` -> `print('')`
- Lines 627-628: `console.log(colors.info(...))` + `console.log(colors.dim(...))` -> `printStatus('info', '[DRY RUN] No changes were made.')` + `printDim('Remove --dry-run flag to apply the migration.')`
- Line 644: `console.log(colors.dim('Migration cancelled.'))` -> `printDim('Migration cancelled.')`
- Lines 653-663: Success output -> `print('')`, `printStatus('success', 'Migration completed successfully!')`, `printDim(...)` for backup/config paths
- Lines 666-671: Failure output -> `print('')`, `printError({ title: 'Migration failed:' })`, loop with `printErr(colors.error(...))`

**runWizard() function (lines 675-745):**

- Lines 676-682: Wizard banner (box-drawing characters) -> Use `print()` for each line to preserve visual while gaining JSON-mode gating. The `colors.info()` calls inside stay (they produce strings).
- Line 685: `console.log(colors.dim(...))` -> `printDim('Detecting your environment...')`
- Line 686: `console.log()` -> `print('')`
- Lines 701-705: Config preview -> `print('')`, `printStatus('info', 'Configuration Preview:')`, `print('')`, `print(formatConfigDisplay(config))`, `print('')`
- Line 723: `console.log(colors.dim('Setup cancelled.'))` -> `printDim('Setup cancelled.')`
- Lines 730-735: Save success -> `print('')`, `printStatus('success', ...)`
- Lines 739-744: Quick start -> `print('')`, `printStatus('info', 'Quick Start:')`, then each `console.log(...)` -> `print(...)`

**displayEnvironment() function (lines 747-808):**

- All `console.log(...)` calls in this function -> `print(...)`. The content uses `colors.success('...')`, `colors.error('...')`, etc. which produce colored strings. `print()` will output them and gate on JSON mode.

**runWizardSteps() function (lines 810-1288):**

- Lines 812-813: `console.log(colors.info('Step 1/4: ...'))` + `console.log()` -> `printStatus('info', 'Step 1/4: Base Configuration')` + `print('')`
- Lines 869-870: `console.log()` + `console.log(colors.info('Step 2/4: ...'))` -> `print('')` + `printStatus('info', 'Step 2/4: Worktree Location')`
- Line 871: `console.log()` -> `print('')`
- Lines 917-918: AI step -> `print('')` + `printStatus('info', 'Step 3/4: AI Integration')` + `print('')`
- Lines 1012-1013: `console.log(colors.dim(...))` -> `printDim(...)`
- Lines 1017-1018: `console.log()` + `console.log(colors.info('Step 4/4: ...'))` -> `print('')` + `printStatus('info', 'Step 4/4: Automation Hooks')`
- Line 1019: `console.log()` -> `print('')`
- Line 1080: `console.log(colors.dim(...))` -> `printDim('No automation hooks available for your environment.')`
- Lines 1083-1084: `console.log()` -> `print('')`
- Lines 1099-1100: Advanced step -> `print('')` + `printStatus('info', 'Step 5: Advanced Configuration')` + `print('')`

**Outer .catch() (lines 73-82):**

- Line 77: `console.log(formatJsonResult(errorResult))` -> KEEP (JSON output)
- Line 79: `console.error(colors.error(...))` -> `printError({ title: message })`

**CRITICAL: Do NOT migrate `console.log(formatJsonResult(...))` or `console.log(JSON.stringify(...))` calls -- those ARE the structured JSON output and must bypass the JSON-mode gate.**
</action>
<verify>
<automated>cd /home/chris/workspace/git-worktree-tools && npx tsc --noEmit && grep -c "initializeLogger" src/cli/wtconfig.ts | xargs -I{} test {} -ge 1 && echo "wtconfig logger OK" && grep -n "console\.\(error\|warn\)" src/cli/wtconfig.ts | grep -v "formatJsonResult\|JSON\.stringify" | head -5; echo "Remaining non-JSON console.error/warn calls above (should be 0)"</automated>
</verify>
<done> - wtconfig.ts calls initializeLogger() with verbose/quiet/noColor/json/commandName flags parsed from process.argv - wtconfig.ts calls setJsonMode(jsonMode) before any output - wtconfig.ts --no-color sets process.env.NO_COLOR and calls setColorEnabled(false) - All ~106 decorative console calls migrated to print/printErr/printStatus/printDim/printError - Only console.log(formatJsonResult(...)) and console.log(JSON.stringify(...)) remain as raw console calls (~10 instances) - TypeScript compiles without errors
</done>
</task>

<task type="auto">
  <name>Task 2: Add setJsonMode to wt/config.ts, migrate console calls, and update tests</name>
  <files>src/cli/wt/config.ts, src/cli/wtconfig.test.ts</files>
  <action>
**INT-06: setJsonMode for wt/config.ts**

1. Add import at top of file: `import { setJsonMode, print, printErr, printDim } from '../../lib/ui/index.js';`
   Note: `printError` and `printStatus` are already imported from `../../lib/ui/index.js` (line 43).

2. In the handler function (line 94), add `setJsonMode(!!argv.json)` as the first line before the switch statement:
   ```typescript
   handler: async (argv) => {
     const subcommand = argv.subcommand || 'interactive';
     const args = argv.args || [];
     setJsonMode(!!argv.json);  // <-- ADD THIS LINE
     // ... rest of switch statement
   ```

**UI primitive migration for wt/config.ts (~53 decorative calls)**

Migrate function by function. Same principle: keep `console.log(formatJsonResult(...))` as-is.

**handleInteractive() (lines 141-161):**

- Line 144: `console.error(colors.error('Not in a git repository.'))` -> `printError({ title: 'Not in a git repository.' })`
- Line 145: `console.log(colors.dim(...))` -> `printDim('Run this command from within a git repository.')`

**handleShow() (lines 166-229):**

- Line 176: JSON output -> KEEP
- Line 180: `console.log(colors.info('Current Configuration'))` -> `printStatus('info', 'Current Configuration')`
- Line 181: `console.log()` -> `print('')`
- Lines 183-184: `console.log(colors.dim(...))` -> `printDim(...)`
- Line 185: `console.log()` -> `print('')`
- Line 187: `console.log(colors.dim(...))` -> `printDim(...)`
- Line 188: `console.log()` -> `print('')`
- Line 228: `console.log(lines.join('\n'))` -> `print(lines.join('\n'))`

**handleGet() (lines 234-270):**

- Lines 254/262: JSON output -> KEEP
- Lines 266-268: Value output -> `print(JSON.stringify(value, null, 2))` and `print(String(value))`

**handleSet() (lines 275-309):**

- Line 284: `console.error(colors.error('Not in a git repository.'))` -> `printError({ title: 'Not in a git repository.' })`
- Lines 292-295: Validation error -> `printError({ title: 'Configuration validation failed:' })` + loop with `printErr(colors.error(...))`
- Lines 298-300: Warnings -> `printErr(colors.warning(...))`
- Line 302: `console.log(colors.success(...))` -> `printStatus('success', ...)`
- Lines 304-306: Catch error -> `printError({ title: \`Failed to set value: ...\` })`

**openInEditor() (lines 352-361):**

- Line 354: `console.log(colors.dim(...))` -> `printDim(...)`
- Line 358: `console.error(colors.error('Failed to open editor'))` -> `printError({ title: 'Failed to open editor' })`

**handleInit() (lines 366-373):**

- Line 367: `console.log(colors.info(...))` -> `printStatus('info', 'Use "wt init" to initialize git-worktree-tools configuration.')`
- Line 368: `console.log()` -> `print('')`
- Lines 369-372: `console.log(colors.dim(...))` -> `printDim(...)`

**handleValidate() (lines 378-432):**

- Lines 390/395/402: JSON output -> KEEP
- Line 410: `console.error(colors.error('Not in a git repository.'))` -> `printError({ title: 'Not in a git repository.' })`
- Line 415: `console.log(colors.success(...))` -> `printStatus('success', ...)`
- Line 416: `console.log(colors.dim(...))` -> `printDim(...)`
- Line 419: `console.log(colors.info(...))` -> `printStatus('info', ...)`
- Line 422: `console.log(colors.success('Configuration loaded successfully.'))` -> `printStatus('success', 'Configuration loaded successfully.')`
- Line 426: `console.log(colors.success('Configuration is valid.'))` -> `printStatus('success', 'Configuration is valid.')`
- Lines 429-431: Error output -> `printErr(colors.error(...))` for each line

**handleMigrate() (lines 437-502):**

- Lines 441/456/468-478: JSON output -> KEEP
- Lines 448-449: `console.error(colors.error(...))` + `console.error(colors.dim(...))` -> `printError({ title: 'Error: Not in a git repository', hint: 'Run this command from within a git repository.' })`
- Line 462: `printStatus('success', ...)` -> already using UI primitive, KEEP
- Line 484: `console.log(formatMigrationReport(...))` -> `print(formatMigrationReport(...))`
- Line 485: `console.log()` -> `print('')`
- Lines 488-493: Success -> `print('')`, `printStatus('success', ...)` (already), `printDim(...)` for backup/config paths
- Lines 495-499: Failure -> `print('')`, `printError({ title: 'Migration failed:' })`, loop `printErr(colors.error(...))`

**handleSchema() (lines 507-524):**

- Line 509: `console.log(colors.info(...))` -> `printStatus('info', 'JSON Schema for .worktreerc')`
- Line 510: `console.log()` -> `print('')`
- Line 511: `console.log(\`URL: ${colors.cyan(schemaUrl)}\`)`->`print(\`URL: ${colors.cyan(schemaUrl)}\`)`
- Line 512: `console.log()` -> `print('')`
- Lines 513-516: `console.log(colors.dim(...))` -> `printDim(...)`
- Line 517: `console.log()` -> `print('')`
- Lines 518-523: The yellow JSON block -> `print(colors.yellow(...))`

**Test updates for src/cli/wtconfig.test.ts:**

- Add test: "calls initializeLogger with parsed flags" -- mock initializeLogger, verify it's called
- Add test: "setJsonMode is NOT called in wtconfig.ts" -- wtconfig.ts is a standalone CLI, it should call setJsonMode. Verify setJsonMode is called.
- Update any existing tests that assert on console.error(colors.error(...)) to assert on printError or mock the UI functions
- If tests use vi.spyOn(console, 'error'), update to mock the UI module instead

After both files are done, verify the `colors` import is still needed in wt/config.ts (it likely is for `colors.dim`, `colors.cyan`, `colors.yellow`, `colors.success`, `colors.info`, `colors.error` used inside string building, not for direct console output).
</action>
<verify>
<automated>cd /home/chris/workspace/git-worktree-tools && npx tsc --noEmit && grep -c "setJsonMode" src/cli/wt/config.ts | xargs -I{} test {} -ge 1 && echo "wt/config setJsonMode OK" && pnpm test -- --run src/cli/wtconfig.test.ts 2>&1 | tail -10</automated>
</verify>
<done> - wt/config.ts calls setJsonMode(!!argv.json) at top of handler - wt/config.ts uses printError/printStatus/printDim/print for all decorative output - wt/config.ts only has console.log(formatJsonResult(...)) as remaining raw console calls - wtconfig.ts calls initializeLogger() with all flags parsed from argv - wtconfig.ts calls setJsonMode(jsonMode) before any output - wtconfig.ts ~106 decorative calls migrated to UI primitives - All tests pass - INT-02, INT-05, INT-06 closed
</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. All tests pass: `pnpm test -- --run`
3. No raw console.error/warn in modified files (excluding JSON output):
   ```
   grep -n "console\.\(error\|warn\)" src/cli/wtconfig.ts src/cli/wt/config.ts | grep -v "formatJsonResult\|JSON\.stringify"
   ```
   Should return 0 lines.
4. setJsonMode present in wt/config.ts: `grep "setJsonMode" src/cli/wt/config.ts` returns a match
5. initializeLogger present in wtconfig.ts: `grep "initializeLogger" src/cli/wtconfig.ts` returns a match
6. Spot-check JSON mode: remaining `console.log` calls should only be `formatJsonResult` or `JSON.stringify` calls
</verification>

<success_criteria>

- INT-02 closed: wtconfig.ts calls initializeLogger() with flag parsing
- INT-05 closed: wtconfig.ts ~106 decorative console calls migrated to UI primitives
- INT-06 closed: wt/config.ts calls setJsonMode(!!argv.json) at top of handler
- wt/config.ts ~53 decorative console calls also migrated to UI primitives
- All existing tests pass, new tests verify the wiring
  </success_criteria>

<output>
After completion, create `.planning/phases/07-legacy-cli-wiring-completeness/07-02-SUMMARY.md`
</output>
