/**
 * Spinner re-export.
 *
 * Provides a single import path for CLI files:
 *   import { withSpinner } from '../ui/index.js'
 *
 * The implementation stays in prompts.ts for backward compatibility.
 */

export { withSpinner } from '../prompts.js';
